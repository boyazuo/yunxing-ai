# 生产环境：api/web 仅绑定本机，由宿主机 Nginx 反向代理
# 使用方式：docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d

services:
  qdrant:
    ports: !reset []

  api:
    ports:
      - "127.0.0.1:8080:8080"

  web:
    ports:
      - "127.0.0.1:3000:3000"
